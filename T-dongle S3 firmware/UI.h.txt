#ifndef UI_H
#define UI_H

#include "Arduino.h"
#include <LovyanGFX.hpp>
#include "Types.h"
#include <vector>

// --- FORWARD DECLARATION of GFX object ---
class LGFX;
extern LGFX gfx;

// --- ОБЪЯВЛЕНИЕ ГЛОБАЛЬНЫХ ПЕРЕМЕННЫХ ---
extern ScreenState currentScreen;
extern bool menuNeedsRedraw;
extern String messageLine1;
extern String messageLine2;
extern unsigned long messageDisplayStartTime;
extern unsigned long usbPrepareStartTime;

extern int mainMenuSelection;
extern const int MAIN_MENU_ITEM_COUNT;
extern int wifiMenuSelection;
extern const int WIFI_MENU_ITEM_COUNT;
extern int wifiScanSelection;

extern String connectingToSsid;
extern unsigned long wifiConnectStartTime;


// --- ОБЪЯВЛЕНИЯ ФУНКЦИЙ ---
void drawScreen();
void drawAttackScreen(const TargetInfo& ap, unsigned long packetCount);

#endif // UI_H