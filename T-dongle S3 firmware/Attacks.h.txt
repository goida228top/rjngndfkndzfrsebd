#ifndef ATTACKS_H
#define ATTACKS_H

#include "Types.h"

// Запускает атаку деаутентификации на выбранную точку доступа.
// Эта функция является блокирующей и управляет циклом атаки и отрисовкой.
// Выход из функции происходит только через перезагрузку устройства.
void startDeauthAttack(const TargetInfo& ap);

#endif // ATTACKS_H
