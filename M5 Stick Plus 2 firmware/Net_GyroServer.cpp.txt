#include "Net_GyroServer.h"
#include "Net_WiFiManager.h"
#include "Globals.h"
#include <WebServer.h>

// --- НОВЫЕ ФУНКЦИИ ДЛЯ ВЕБ-СЕРВЕРА ГИРОСКОПА ---

const char* GYRO_HTML = R"rawliteral(
<!DOCTYPE html>
<html>
<head>
    <title>M5-Nexus IMU 3D</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body { margin: 0; background-color: #1e1e1e; color: #d4d4d4; overflow: hidden; font-family: monospace; }
        canvas { display: block; }
        #info { position: absolute; top: 10px; width: 100%; text-align: center; z-index: 100; }
    </style>
</head>
<body>
    <div id="info">M5-Nexus IMU Visualization</div>

    <script type="module">
        import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.166.1/build/three.module.js';

        let scene, camera, renderer, box;

        function init() {
            // Scene
            scene = new THREE.Scene();

            // Camera
            camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            camera.position.z = 5;

            // Renderer
            renderer = new THREE.WebGLRenderer({ antialias: true });
            renderer.setSize(window.innerWidth, window.innerHeight);
            document.body.appendChild(renderer.domElement);

            // Box (representing M5Stick)
            const geometry = new THREE.BoxGeometry(1.2, 2.5, 0.6); // Proportions similar to M5StickC
            const material = new THREE.MeshStandardMaterial({ color: 0xfc9803, roughness: 0.5 }); // Orange color
            box = new THREE.Mesh(geometry, material);
            scene.add(box);

            // Lights
            const ambientLight = new THREE.AmbientLight(0xffffff, 0.8);
            scene.add(ambientLight);
            const directionalLight = new THREE.DirectionalLight(0xffffff, 1.2);
            directionalLight.position.set(2, 5, 5);
            scene.add(directionalLight);

            // Handle window resize
            window.addEventListener('resize', onWindowResize, false);
            
            // Start fetching data and animating
            fetchData();
            animate();
        }

        function onWindowResize() {
            camera.aspect = window.innerWidth / window.innerHeight;
            camera.updateProjectionMatrix();
            renderer.setSize(window.innerWidth, window.innerHeight);
        }

        async function fetchData() {
            try {
                const response = await fetch('/data');
                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
                const data = await response.json();
                
                // --- Rotation Logic ---
                // We use accelerometer data to determine tilt (orientation relative to gravity)
                // The firmware maps axes for us:
                // ay = forward/backward tilt
                // ax = left/right tilt
                
                // Convert G-force component to angle in radians
                const rotX = Math.asin(Math.max(-1, Math.min(1, -data.ay))); // Pitch
                const rotY = Math.asin(Math.max(-1, Math.min(1, data.ax)));  // Roll
                
                // Apply rotation using a Quaternion for smooth interpolation and to avoid gimbal lock
                const targetQuaternion = new THREE.Quaternion().setFromEuler(new THREE.Euler(rotX, rotY, 0, 'XYZ'));
                box.quaternion.slerp(targetQuaternion, 0.1); // slerp for smooth transition

            } catch (error) {
                console.error('Error fetching IMU data:', error);
                document.getElementById('info').innerText = "Connection Error - Check Console";
            }
            // Fetch again after a short delay
            setTimeout(fetchData, 100);
        }

        function animate() {
            requestAnimationFrame(animate);
            renderer.render(scene, camera);
        }

        init();
    </script>
</body>
</html>
)rawliteral";

void handleGyroRoot() {
    server.send(200, "text/html", GYRO_HTML);
}

void handleGyroData() {
    if (M5.Imu.update()) {
        auto data = M5.Imu.getImuData();
        
        // Переназначаем оси для интуитивного соответствия экрану
        // X: наклон влево/вправо -> IMU Y
        // Y: наклон вперед/назад -> IMU -X
        // Z: гравитация вверх/вниз -> IMU -Z
        float ax = data.accel.y;
        float ay = -data.accel.x;
        float az = -data.accel.z;
        float gx = data.gyro.y;
        float gy = -data.gyro.x;
        float gz = -data.gyro.z;

        char jsonBuffer[200];
        snprintf(jsonBuffer, sizeof(jsonBuffer), 
                 "{\"ax\":%.2f,\"ay\":%.2f,\"az\":%.2f,\"gx\":%.2f,\"gy\":%.2f,\"gz\":%.2f}",
                 ax, ay, az, gx, gy, gz);
        
        server.send(200, "application/json", jsonBuffer);
    } else {
        server.send(503, "application/json", "{\"error\":\"IMU data not available\"}");
    }
}

void startGyroServer() {
    if (isWifiConnected()) {
        server.on("/", HTTP_GET, handleGyroRoot);
        server.on("/data", HTTP_GET, handleGyroData);
        server.begin();
        Serial.println("Gyroscope web server started.");
    } else {
        Serial.println("Cannot start gyro server, WiFi not connected.");
    }
}

void stopGyroServer() {
    server.stop();
    Serial.println("Gyroscope web server stopped.");
}

void handleGyroServerClient() {
    server.handleClient();
}
