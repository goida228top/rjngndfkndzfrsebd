#ifndef TYPES_H
#define TYPES_H

#include <Arduino.h>

// Режимы меню
enum Mode {
  MODE_MAIN_MENU,
  MODE_WIFI_MENU,
  MODE_WIFI_CONFIG,
  MODE_CONNECTING,
  MODE_TIME_SYNCING,
  MODE_CONNECTION_FAILED,
  MODE_MESSAGE_STANDBY,
  MODE_VOICE_RECORDING,
  MODE_GEMINI_RECORDING,
  MODE_GOOGLE_SEARCH_RECORDING,
  MODE_GEMINI_FOLLOWUP_RECORDING,
  MODE_DIAGNOSTICS,
  MODE_GYROSCOPE,
  MODE_STEALTH_CLOCK,
  MODE_BUZZER
};

#endif // TYPES_H