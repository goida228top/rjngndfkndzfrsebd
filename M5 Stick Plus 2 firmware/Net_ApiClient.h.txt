#ifndef NET_API_CLIENT_H
#define NET_API_CLIENT_H

#include <Arduino.h>

void handleNetwork(); // The poller
void checkForNewMessages();
bool sendRecordedVoiceHTTP(uint8_t* audio_buffer, size_t buffer_len);
bool sendVoiceToGemini(uint8_t* audio_buffer, size_t buffer_len);
bool sendFollowUpToGemini(uint8_t* audio_buffer, size_t buffer_len, String previous_response);

#endif // NET_API_CLIENT_H
